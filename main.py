import json

# with open("tasks.json", "r") as file:
#     tasks = json.load(file)

# tasks.append({"title": "Make a project", "is_done": False})

# with open("tasks.json", "w") as file:
#     json.dump(tasks, file, indent=4)

